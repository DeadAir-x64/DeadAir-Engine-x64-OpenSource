-- Глобалы, которые читаются и нигде не присваиваются, и это ДЕФЕКТЫ САМОГО МОДА, а не порта.
-- Найдены проверкой globals; лежат здесь, чтобы набор оставался зелёным, но находки не терялись.
-- Каждая строка сопровождается разбором: почему не чинится молча.

falout_manager   -- опечатка вместо fallout_manager, bind_stalker_ext.script:221 в actor_on_save.
                 -- Строкой ниже в actor_on_load написано правильно. МИНА, НЕ активная: обе функции
                 -- начинаются с `if (USE_MARSHAL) then return end`, а marshal в порте есть
                 -- (Externals/xrLuaFix/lua-marshal, в логе "using marshal library"), поэтому до
                 -- опечатки исполнение не доходит. Если marshal когда-нибудь пропадёт, КАЖДОЕ
                 -- сохранение начнёт падать на этой строке, а загрузка — читать из пакета блок,
                 -- который никто не записал.

fasle            -- опечатка вместо false: ui_sleep_dialog.script:106, `local radiation = fasle`.
                 -- Рядом закомментировано `db.actor.radiation > 0`. Последствие мягкое — nil так же
                 -- ложен, как false, — но предупреждение о сне под облучением не показывается.

not_is_found     -- check_logic_path.script:123, `if cnt == 1 and not_is_found then`. Похоже на
                 -- потерянный пробел в `not is_found`. Ветка не срабатывает никогда.

if_then_else     -- ph_car.script: вспомогательной функции с таким именем в моде нет вовсе, вызов
                 -- уронил бы обработчик. Спит: машины в Dead Air не используются.
