-- Вызовы level.*, которых в порте нет, и это разобрано.
-- На всё остальное проверка engine ругается: так ловится «мод зовёт — порт не отдал».

actor  -- ph_hit.script:44, `level.actor()`. Дефект МОДА, не пробел порта: такого биндинга нет ни
       -- у автора DA, ни в базе Call of Chernobyl. Спит: схема ph_hit зарегистрирована в
       -- modules.script, но ни один уровень её не использует. Лечится заменой на db.actor.
