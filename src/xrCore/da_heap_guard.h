#pragma once

// [DA_PORT] Контроль кучи для ИГРАБЕЛЬНОЙ сборки: отравление, карантин, ловля двойного освобождения.
//
// ЗАЧЕМ ЭТО ВООБЩЕ ЕСТЬ. Настоящий AddressSanitizer нам недоступен там, где нужен. В MinGW нет
// libasan (компоновка встаёт на `cannot find -lasan`), под Linux ASan работает, но игру там не
// запустить — рендер R4 существует только под DX11, а ветка MSVC у нас не собирается: решение
// src/engine.sln не правилось с 25.06.2026 и наших файлов не содержит вовсе.
//
// ⛔ ЭТО НЕ ЗАМЕНА ASan. Он не ловит выход за границы в момент записи, не видит стек выделения и
// ничего не знает про стек и глобальные объекты. Он закрывает РОВНО ОДИН класс — обращение к
// освобождённой памяти — зато в живой игре, на нашей быстрой сборке и без прав администратора.
//
// ПОЧЕМУ ИМЕННО ЭТОТ КЛАСС. Оба открытых вылета выглядят одинаково: разыменование по адресу вида
// null+смещение (#70: null+0x2CC в xrGame) и чтение уничтоженной геометрии столкновений (#66).
// Различить «поле никогда не заполняли» и «объект уже освободили» по такому адресу НЕЛЬЗЯ, а лечение
// у них разное. Отравление делает эти два случая различимыми с одного взгляда на лог.
//
// КАК ЭТО РАБОТАЕТ, тремя приёмами:
//
//   1. ОТРАВЛЕНИЕ. Освобождаемый блок забивается байтом 0xDD. Указатель, прочитанный из мёртвого
//      объекта, становится 0xDDDDDDDDDDDDDDDD, и обращение по нему падает сразу — с адресом, который
//      ни с чем не спутать. Число, прочитанное оттуда же, выглядит явно недостоверным.
//
//   2. КАРАНТИН. Отравленный блок НЕ отдаётся куче сразу, а лежит в очереди, пока та не переполнится.
//      Это главная часть. Без неё блок переиспользуется через микросекунды, чужой живой объект
//      ложится поверх мёртвого, и падение приезжает далеко от ошибки — ровно то, из-за чего оба
//      вылета до сих пор не разобраны. Карантин растягивает окно с микросекунд до минут.
//
//   3. ДВОЙНОЕ ОСВОБОЖДЕНИЕ. Первые 16 байт отравленного блока хранят метку и адрес его самого.
//      Повторное освобождение видно сразу — и оно сообщается в лог со стеком, а не портит кучу
//      молча, чтобы всплыть позже в непричастном месте.
//
// ВКЛЮЧЕНИЕ — только переменной окружения, и это не лень. Консольная команда появляется на свет
// многократно позже первого выделения: к моменту, когда её можно было бы выполнить, интересующие
// нас объекты уже прожили жизнь.
//
//   set DA_HEAP_GUARD=1        включить
//   set DA_HEAP_GUARD_MB=64    карантин НА ПОТОК в мегабайтах (по умолчанию 64)
//
// ЦЕНА. Одно заполнение памяти на каждое освобождение плюс отложенный возврат: расход памяти растёт
// на размер карантина, кадры становятся заметно тяжелее. Это сборка для поиска ошибок, а не для
// замеров производительности — по ней НЕЛЬЗЯ судить о FPS.
//
// ⛔ Работает только при USE_PURE_ALLOC (у нас MEMORY_ALLOCATOR=standard). При mimalloc весь модуль
// выключается на этапе компиляции: там свой учёт размеров, и _msize к его блокам неприменим.

#include "xrCore/xrCore.h"

// Чем забивается освобождённая память. Вынесено в заголовок намеренно: это ДОГОВОР, а не деталь —
// именно это число ищут в логе падения и именно его проверяет самопроверка стенда.
//
// ⚠️ Значение подобрано под x86-64, а не взято красивым. Сплошной 0xDD дал бы указатель
// 0xDDDDDDDDDDDDDDDD, а такой адрес **неканонический**: процессор выдаёт общий отказ защиты вместо
// отказа страницы, и настоящего адреса в отчёте НЕ БУДЕТ — Windows напишет `ffffffffffffffff`.
// Проверено живой игрой: падение случилось, но опознать в нём обращение к мертвецу было нельзя.
//
// Здесь старшие 17 бит нулевые (адрес канонический), сама величина около 138 ТБ — заведомо выше
// любых настоящих выделений, поэтому отказ приходит честной страницей. Байтами в памяти это
// по-прежнему почти сплошной 0xDD, а четырёхбайтовое чтение даёт привычное 0xDDDDDDDD.
constexpr u64 DA_HEAP_POISON_WORD = 0x00007DDDDDDDDDDDull;

// Быстрый путь: когда контроль выключен, всё сводится к чтению одного логического значения.
// Определение и пояснение — в da_heap_guard.cpp.
XRCORE_API bool da_heap_guard_enabled();

// Освобождение под контролем. Возвращает true, если блок принят в карантин и звать free() НЕ НАДО.
// false означает «контроль выключен или блок ему не подходит» — освобождать обычным путём.
XRCORE_API bool da_heap_guard_free(void* ptr);

// Перевыделение под контролем: результат в out, возвращает true, если сделано здесь.
//
// ЗАЧЕМ ОТДЕЛЬНО. Обычный realloc, когда блок не удаётся расширить на месте, копирует данные и
// отдаёт старый блок куче САМ — мимо нас, без отравления и без карантина. А через этот путь идёт
// вся память LuaJIT: lua_alloc зовёт xr_realloc даже на первое выделение. Оставить его без контроля
// значило бы оставить без контроля половину игры.
XRCORE_API bool da_heap_guard_realloc(void* ptr, size_t size, void** out);

// Отчёт в лог: сколько блоков и байт под карантином, сколько поймано двойных освобождений.
XRCORE_API void da_heap_guard_stat();

// Сколько двойных освобождений поймано за прогон.
//
// ЗАЧЕМ ОТДЕЛЬНО ОТ ОТЧЁТА. Сам отчёт идёт через Msg, то есть в лог. Прогонному стенду лог не
// поднимают (файловая система ему не нужна), и там сообщение о находке просто пропало бы — а
// «прибор промолчал» неотличимо от «прибор ничего не нашёл». Число проверяется прямо в коде.
XRCORE_API u64 da_heap_guard_double_frees();
