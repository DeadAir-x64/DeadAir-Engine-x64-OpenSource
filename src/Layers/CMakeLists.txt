add_subdirectory(xrAPI)

if (WIN32)
    add_subdirectory(xrRenderPC_R4)
endif()

# [DA_PORT] OpenGL-рендерер убран из сборки целиком.
# Каждая правка этого порта — временные апскейлеры, векторы движения, реактивная маска,
# потолок теневых ламп, гамма и тени, весь диагностический набор — лежит в xrRenderPC_R4,
# и играется только он. GL при этом ещё и не компилировался: в его ветку не переносились
# rt_Velocity/rt_Reactive и сигнатура CDetailManager::hw_Render_dump, из-за чего сборка
# «всего» падала, а собирать приходилось поимённым списком целей.
# Исходники Layers/xrRenderPC_GL и Layers/xrRenderGL оставлены в дереве, просто не собираются.
